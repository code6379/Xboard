services:
  xboard:
    image: ghcr.io/cedar2025/xboard:latest
    restart: unless-stopped
    ports:
      - "7001:7001"
    volumes:
      - ./www:/www
      - ./data/redis:/data
      - ./data/database:/www/.docker/.data
      - ./data/logs:/www/storage/logs
      - ./data/theme:/www/storage/theme
      - ./data/plugins:/www/plugins
    environment:
      - RESOURCE_PROFILE=balanced
      - ENABLE_HORIZON=true
      - docker=true
